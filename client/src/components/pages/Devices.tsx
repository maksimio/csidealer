import { FC } from 'react'
import { Container } from '@chakra-ui/react'

const Devices: FC = () => {
  return (
    <Container maxW={'full'}>
      Устройства
    </Container>
  )
}

export default Devices
